# Python 3.9+ recommended (stdlib zoneinfo).
# Install: pip install -r requirements.txt
# External tools (not pip): ffmpeg, mkvpropedit, N_m3u8DL-RE — install separately and/or set paths in config.yaml.

PyYAML>=6.0
requests>=2.28.0
rich>=13.0
yt-dlp>=2026.3.17
xmltodict>=0.13.0
pywidevine>=1.0.0
filedate>=2.0.0
piexif>=1.1.3
tzdata>=2025.1
